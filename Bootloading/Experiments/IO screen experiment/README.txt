This directory is used to experiment with using memory-mapped and port-based input/output. C code is located in the "C" directory, assembly code - in "Assembly", and object files are stored in "Object".
Makefile is set up, so to generate an ISO file, just type '>wsl make' in cmd
