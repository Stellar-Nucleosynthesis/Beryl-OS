This directory is used to experiment with calling C code from assembly. Also, automatic project building is used here for the first time. Assembly code sets up runtime environment and calls a C function kmain.
Makefile is set up, so to set everything up, just type '>wsl make' in cmd
